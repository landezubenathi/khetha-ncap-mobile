import { Link } from 'expo-router';
import { ScrollView, Text, View } from 'react-native';
import { colors, spacing } from '../../src/theme';
export default function Journey() { return <ScrollView style={{ backgroundColor: colors.bg }} contentContainerStyle={{ padding: spacing.md }}><Text style={{ fontSize: 28, fontWeight: '800', color: colors.navy }}>My journey</Text><Text style={{ color: colors.muted, marginTop: 6 }}>Small steps add up to a future you choose.</Text>{['Complete your career questionnaire','Explore your top career matches','Find a qualification','Save a provider'].map((x,i) => <View key={x} style={{ flexDirection: 'row', marginTop: 26 }}><View style={{ width: 34, height: 34, borderRadius: 17, backgroundColor: i === 0 ? colors.teal : colors.border, alignItems: 'center', justifyContent: 'center' }}><Text style={{ color: i === 0 ? colors.white : colors.muted, fontWeight: '800' }}>{i + 1}</Text></View><View style={{ marginLeft: 14, flex: 1 }}><Text style={{ color: colors.navy, fontWeight: '800', fontSize: 16 }}>{x}</Text><Text style={{ color: colors.muted, marginTop: 5 }}>{i === 0 ? 'Ready to start' : 'Unlock after your next step'}</Text></View></View>)}<Link href="/profile" style={{ color: colors.blue, marginTop: 34 }}>Need human guidance? Contact Khetha →</Link></ScrollView>; }
