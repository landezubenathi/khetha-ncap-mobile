import { Pressable, ScrollView, Text, View } from 'react-native'; import { useLocalSearchParams } from 'expo-router'; import { colors, spacing } from '../../src/theme'; import { useUserStore } from '../../src/store/user';
export default function Career() { const { id } = useLocalSearchParams<{id:string}>(); const saved=useUserStore(s=>s.saved); const toggle=useUserStore(s=>s.toggleSaved); const isSaved=saved.includes(id || 'health-scientist'); return <ScrollView style={{ backgroundColor: colors.bg }} contentContainerStyle={{ padding: spacing.md }}><Text style={{ color: colors.teal, fontWeight: '800' }}>CAREER MATCH</Text><Text style={{ color: colors.navy, fontSize: 30, fontWeight: '800', marginTop: 8 }}>{id === 'qualification' ? 'Bachelor of Health Sciences' : id === 'provider' ? 'University of South Africa' : 'Health Scientist'}</Text><Text style={{ color: colors.ink, fontSize: 16, lineHeight: 25, marginTop: 18 }}>Use your curiosity and care for people to improve lives. Explore the subjects, qualifications and skills that can help you get there.</Text><View style={{ backgroundColor: colors.white, padding: spacing.md, borderRadius: 14, marginTop: 22 }}><Text style={{ color: colors.navy, fontWeight: '800', fontSize: 18 }}>Your pathway</Text><Text style={{ color: colors.muted, marginTop: 12 }}>Recommended subjects: Mathematics, Life Sciences, Physical Sciences</Text><Text style={{ color: colors.muted, marginTop: 10 }}>Study level: Diploma or degree</Text><Text style={{ color: colors.muted, marginTop: 10 }}>Skills: Research · Communication · Analysis</Text></View><Pressable onPress={()=>toggle(id || 'health-scientist')} style={{ backgroundColor: isSaved ? colors.border : colors.blue, borderRadius: 12, padding: 16, marginTop: 20 }}><Text style={{ textAlign: 'center', color: isSaved ? colors.navy : colors.white, fontWeight: '800' }}>{isSaved ? 'Saved ✓' : 'Save this for later'}</Text></Pressable></ScrollView>; }
