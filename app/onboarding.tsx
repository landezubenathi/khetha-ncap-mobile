import { Stack, router } from 'expo-router';
import { useState } from 'react';
import { Pressable, SafeAreaView, Text, View } from 'react-native';
import { colors, spacing } from '../src/theme';
import { useUserStore } from '../src/store/user';

export default function Onboarding() { const [language, setLanguage] = useState('English'); const complete = useUserStore(s => s.completeOnboarding); return <SafeAreaView style={{ flex: 1, backgroundColor: colors.navy }}><Stack.Screen options={{ headerShown: false }} /><View style={{ flex: 1, padding: spacing.xl, justifyContent: 'center' }}><Text style={{ color: colors.yellow, fontSize: 18, fontWeight: '800' }}>KHETHA NCAP</Text><Text style={{ color: colors.white, fontSize: 40, fontWeight: '800', marginTop: 20 }}>Your future starts with a choice.</Text><Text style={{ color: '#D9E2EC', fontSize: 18, lineHeight: 27, marginTop: 18 }}>Explore careers, choose subjects and find study opportunities — even when you are offline.</Text><Text style={{ color: colors.white, fontWeight: '700', marginTop: 40 }}>Choose your language</Text><View style={{ flexDirection: 'row', gap: 10, marginTop: 12 }}>{['English', 'isiZulu', 'Sesotho'].map(x => <Pressable key={x} onPress={() => setLanguage(x)} style={{ borderWidth: 1, borderColor: language === x ? colors.yellow : '#829AB1', borderRadius: 24, padding: 12 }}><Text style={{ color: colors.white }}>{x}</Text></Pressable>)}</View><Pressable onPress={() => { complete(); router.replace('/(tabs)'); }} style={{ backgroundColor: colors.yellow, padding: 18, borderRadius: 14, marginTop: 42, alignItems: 'center' }}><Text style={{ color: colors.navy, fontWeight: '800', fontSize: 16 }}>Start my journey</Text></Pressable></View></SafeAreaView>; }
