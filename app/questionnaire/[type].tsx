import { useState } from 'react'; import { Pressable, ScrollView, Text, View } from 'react-native'; import { useLocalSearchParams, router } from 'expo-router'; import { colors, spacing } from '../../src/theme';
const questions = { subjects: ['Which subjects do you enjoy most?','What kind of problems do you like solving?','Which learning environment suits you?'], career: ['I enjoy helping people solve problems.','I like creating or building things.','I enjoy working with numbers and information.','I prefer practical, hands-on work.'], 'job-fit': ['I stay calm when plans change.','I enjoy collaborating with a team.','I am curious and keep learning.'] };
export default function Questionnaire() { const { type } = useLocalSearchParams<{type:string}>(); const qs = questions[type as keyof typeof questions] || questions.career; const [step,setStep] = useState(0); const [answers,setAnswers] = useState<number[]>([]); const finish = () => router.replace('/career/health-scientist'); return <ScrollView style={{ backgroundColor: colors.bg }} contentContainerStyle={{ padding: spacing.md }}><Text style={{ color: colors.muted }}>Question {step + 1} of {qs.length}</Text><Text style={{ color: colors.navy, fontSize: 26, fontWeight: '800', marginTop: 14 }}>{qs[step]}</Text>{['Not at all','A little','Somewhat','Very much'].map((x,i) => <Pressable key={x} onPress={() => { const a=[...answers,i]; setAnswers(a); step < qs.length-1 ? setStep(step+1) : finish(); }} style={{ backgroundColor: colors.white, borderWidth: 1, borderColor: colors.border, padding: 18, borderRadius: 12, marginTop: 14 }}><Text style={{ color: colors.ink, fontWeight: '700' }}>{x}</Text></Pressable>)}</ScrollView>; }
